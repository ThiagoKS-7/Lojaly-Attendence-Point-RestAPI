{
    "private": true,
    "scripts": {
        "dev": "vite",
        "start": " docker compose up -d && php artisan serve",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
